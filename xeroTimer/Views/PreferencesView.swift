import SwiftUI

struct PreferencesView: View {
    var body: some View {
        Form {
            Text("Preferences")
            // Add preference controls here
        }
        .padding()
        .frame(width: 400, height: 300)
    }
} 